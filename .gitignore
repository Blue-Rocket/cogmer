bin/
*.db
*.db-wal
*.db-shm

# CodeGraph index — rebuilt from source, machine-specific
.codegraph/

# The compiled binary. Rebuilt by `go build`; 17MB per commit otherwise.
/claude-team

dist/

# Personal, per-machine Claude Code settings. Tracked settings would impose
# whatever tooling they name on anybody who clones this.
.claude/settings.local.json
